'use strict'; 
Seattle {

minCustomr:   ;
maxCustomer:  ;
avgCookies:   ;

this. = randomValue(3,12) + ' months';

};

function randomValue(min, max) {
    return Math.floor(Math.random() * (max - min + 1) + min); //The maximum is inclusive and the minimum is inclusive
}







